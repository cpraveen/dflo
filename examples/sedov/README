Extract solution along line

$ visit -cli -nowin -s lineout_extract.py

This creates the files
   Density.curve
   Pressure.curve
   Energy.curve

Run sedov3.f

$ gfortran sedov3.f
$ ./a.out

Plot using gnuplot

$ gnuplot plot.gnu

Open sedov.eps file

$ gv sedov.eps
